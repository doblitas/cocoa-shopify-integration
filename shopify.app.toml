# Configuración de la app para Shopify CLI (shopify app dev / shopify app deploy).
# 1) Sustituye client_id por el mismo valor que NEXT_PUBLIC_SHOPIFY_API_KEY (Client ID en Partners / dev dashboard).
# 2) Si cambias de dominio, actualiza application_url, [auth].redirect_urls y (opcional) SHOPIFY_APP_HOST en Vercel.

name = "Cocoa Integration"
client_id = "REPLACE_WITH_CLIENT_ID"
application_url = "https://cocoa-shopify-integration.vercel.app/dashboard"
embedded = true

[access_scopes]
scopes = "read_products,write_products,read_inventory"

[auth]
redirect_urls = [
  "https://cocoa-shopify-integration.vercel.app/api/auth/callback",
]

[webhooks]
api_version = "2024-10"

[[webhooks.subscriptions]]
topics = [ "products/create", "products/update" ]
uri = "/api/webhooks/shopify/products"

[build]
automatically_update_urls_on_dev = false
